import { Exercise } from './exercise.model';

describe('Exercise', () => {
  // new Exercise();
  // e.name = 'test';
  // e
  xit('should create an instance', () => {
    // expect(new Exercise().name);
  });
});
